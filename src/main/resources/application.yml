server:
  port: 8300

spring:
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: filesystem
      port: 27017
      host: localhost
      auto-index-creation: true